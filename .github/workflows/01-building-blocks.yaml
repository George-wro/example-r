name: "01 – Building Blocks"
on: push, workflow_dispatch

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: "Say hello": echo "Hello, World!"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name : "failed step": 
        run: |
          echo "I"
          echo "will"
          echo "fail"
      - name: "say goodbye": echo "Goodbye!"