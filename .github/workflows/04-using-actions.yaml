name: "04 – Using Actions"
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v4
      - name: "Printing folders"
        run : |
          ls -la
          